<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:p="clr-namespace:Playnite;assembly=Playnite"
                    xmlns:popt="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options" 
                    mc:Ignorable="d">

    <Style TargetType="Border" x:Key="VisualBrushBorderMask">
        <Setter Property="CornerRadius" Value="{DynamicResource ControlCornerRadiusLarge}" />
        <Setter Property="Visibility" Value="{Binding RelativeSource={RelativeSource Self}, Path=Tag, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="False">
                <Setter Property="CornerRadius" Value="0,0,0,0" />
            </DataTrigger>
            <DataTrigger Binding="{Binding ElementName=ThemeSwitchBorderMask, Path=Tag, Mode=OneWay}" Value="True">
                <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding ElementName=ThemeSwitchBorderMask, Path=Tag, Mode=OneWay}" Value="False">
                <Setter Property="Background" Value="{DynamicResource DarkControlBackgroundBrush}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="GridViewItemTemplate" TargetType="{x:Type GameListItem}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type GameListItem}">
                    <Border BorderThickness="{Settings GridItemMargin}" BorderBrush="{DynamicResource GridItemBackgroundBrush}">
                        
                        <StackPanel Name="PART_PanelHost"
                                    d:DataContext="{x:Static DesignMainViewModel.DesignSelectedGameIntance}">

                            <Grid x:Name="TileContainer" Width="{Settings GridItemWidth}"
								  ClipToBounds="True"
								  Tag="{DynamicResource GridViewGameCoverShowPlatformBanner}">
								<Grid.Style>
									<Style TargetType="Grid">
										<Setter Property="Height" Value="{Binding Width, RelativeSource={RelativeSource Self}}" />
										<Style.Triggers>
											<DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="True">
												<Setter Property="Height" Value="{Binding Width, RelativeSource={RelativeSource Self}, Converter={PluginConverter Plugin=ThemeExtras, Converter=MultiplyConverter}, ConverterParameter=1.1665}" />
											</DataTrigger>
										</Style.Triggers>
									</Style>
								</Grid.Style>

                                <Border Style="{StaticResource VisualBrushBorderMask}" Name="Mask" Tag="{DynamicResource GridViewGameCoverUseRoundedCorners}">
                                    <TextBlock x:Name="ThemeSwitchBorderMask" Visibility="Collapsed" Tag="{DynamicResource ThemeDarkStyle}" />
                                </Border>

                                <StackPanel>
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=Mask, Path=Tag}" Value="True">
                                                    <Setter Property="OpacityMask">
                                                        <Setter.Value>
                                                            <VisualBrush Visual="{Binding ElementName=Mask}" />
                                                        </Setter.Value>
                                                    </Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    <Grid Height="{Binding ActualHeight, ElementName=TileContainer}">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="*" />
                                        </Grid.RowDefinitions>
                                        <ContentControl x:Name="ThemeExtras_Banner" Grid.Row="0" RenderOptions.BitmapScalingMode="Fant"
                                                Width="{Settings GridItemWidth}"
                                                VerticalAlignment="Top" HorizontalAlignment="Center"
                                                Tag="{DynamicResource GridViewGameCoverShowPlatformBanner}"
                                                Visibility="{Binding Tag, RelativeSource={RelativeSource Self}, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                        <Image Name="PART_ImageCover" Grid.Row="1" SnapsToDevicePixels="True" HorizontalAlignment="Center"
                                                VerticalAlignment="Center" RenderOptions.BitmapScalingMode="Fant" StretchDirection="Both" Stretch="{Settings CoverArtStretch}" />
                                        <Grid Grid.RowSpan="2" Tag="{DynamicResource GridViewGameCoverShowFavorite}"
                                                Visibility="{Binding Tag, RelativeSource={RelativeSource Self}, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>

                                            <Viewbox VerticalAlignment="Top" HorizontalAlignment="Right" Grid.Column="4">
                                                <Grid>
                                                    <Grid.Style>
                                                        <Style TargetType="Grid">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Favorite}" Value="True">
                                                                    <Setter Property="Visibility" Value="Visible" /> 
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Grid.Style>
                                                    <Border Background="{DynamicResource ControlBackgroundBrush}" Width="32" Height="32" CornerRadius="16"
                                                            Margin="0,8,8,0" Opacity="0.6" />
                                                    <TextBlock Text="&#xe00a;" Foreground="White" Margin="6,14,14,6" Style="{DynamicResource IconFontStyle}" />
                                                </Grid>
                                            </Viewbox>
                                        </Grid>
                                    </Grid>
                                </StackPanel>

                                <!--Game Not Installed-->
                                <Border x:Name="TextGameName" CornerRadius="{Binding ElementName=Mask, Path=CornerRadius}" Visibility="Collapsed">
                                    <TextBlock Text="{Binding DisplayName}" TextWrapping="Wrap" Margin="8" Style="{StaticResource BaseTextBlockStyle}"
                                            TextTrimming="CharacterEllipsis" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>

                                <Border x:Name="BorderMouseOver" Background="{DynamicResource DarkControlBackgroundBrush}"
                                        CornerRadius="{Binding ElementName=Mask, Path=CornerRadius}" Opacity="0" />

                                <Border x:Name="SelectedGameBorder" Background="{DynamicResource GridViewGameCoverSelectedBackgroundBrush}"
                                        CornerRadius="{Binding ElementName=Mask, Path=CornerRadius}" Opacity="0">
                                    <Grid Margin="4">
                                        <Grid.Style>
                                            <Style TargetType="Grid">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=Mask, Path=Tag}" Value="True">
                                                        <Setter Property="OpacityMask">
                                                            <Setter.Value>
                                                                <VisualBrush Visual="{Binding ElementName=Mask}" />
                                                            </Setter.Value>
                                                        </Setter>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Grid.Style>
                                        <Rectangle Stroke="#3DFFFFFF" StrokeDashArray="6,3" StrokeThickness="1" RadiusX="{Binding ElementName=Mask, Path=CornerRadius.TopLeft}"
                                                RadiusY="{Binding ElementName=Mask, Path=CornerRadius.TopLeft}" />
                                        <Canvas IsHitTestVisible="False">
                                            <Line X1="0" Y1="0" X2="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Grid}}"
                                                    Y2="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Grid}}" Stroke="#3DFFFFFF" StrokeThickness="1" />
                                        </Canvas>
                                    </Grid>
                                </Border>

                                <Border x:Name="GlowPlayingBorder" Visibility="Collapsed" Tag="False" CornerRadius="{Binding ElementName=Mask, Path=CornerRadius}"
                                        Style="{DynamicResource CoverIsPlayingAnimatedBackground}" />

                                <TextBlock Name="EnablePlayingGlowBorder" Visibility="Collapsed" Tag="{DynamicResource GridViewEnablePlayingGlowBorder}" />

                                <Border Name="ShineBackground" Tag="False" IsHitTestVisible="False" CornerRadius="{Binding ElementName=Mask, Path=CornerRadius}"
                                        Style="{DynamicResource AnimatedCoverShineBackground}" />

                                <TextBlock Name="EnableHighlightAnimation" Visibility="Collapsed" Tag="{DynamicResource GridViewGameCoverShowShineAnimation}" />

                                <!--
                                    Completion trophy. Always visible, unlike the hover controls,
                                    because the point is to see at a glance what has been finished
                                    while scanning the library.

                                    Drawn as one Path rather than an icon-font glyph: the tiers
                                    need distinct fills and an outline state, and a single Path
                                    means one Fill and one Stroke to set per status instead of
                                    juggling several elements.

                                    Gold for played, then gems above it: emerald for beaten,
                                    ruby for completed. Finishing a game is the achievement worth
                                    rewarding, so the top two tiers outrank the metal rather than
                                    continuing it. Flat single colours, not gradients, because a
                                    multicoloured 28px icon reads as mud.

                                    Playing and on hold are outlines: in progress, nothing earned.
                                    Abandoned is a dulled grey. Nothing shows at all for not played,
                                    plan to play, or no status: an empty shelf rather than a
                                    participation award.
                                -->
                                <Grid Name="TrophyOverlay">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Viewbox Grid.Column="3" Grid.ColumnSpan="2" VerticalAlignment="Bottom" HorizontalAlignment="Right">
                                        <Path Width="24" Height="24" Stretch="Uniform" Margin="6,6,8,8" StrokeThickness="1.4"
                                              Data="M6,3 H18 V10 C18,13.3 15.3,16 12,16 C8.7,16 6,13.3 6,10 Z
                                                    M11,16 H13 V19 H11 Z
                                                    M7.5,19 H16.5 V21 H7.5 Z
                                                    M6,4 H4 C2.3,4 1,5.3 1,7 C1,8.7 2.3,10 4,10 H6 V8.5 H4 C3.2,8.5 2.5,7.8 2.5,7 C2.5,6.2 3.2,5.5 4,5.5 H6 Z
                                                    M18,4 H20 C21.7,4 23,5.3 23,7 C23,8.7 21.7,10 20,10 H18 V8.5 H20 C20.8,8.5 21.5,7.8 21.5,7 C21.5,6.2 20.8,5.5 20,5.5 H18 Z">
                                            <Path.Style>
                                                <Style TargetType="Path">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Setter Property="Effect">
                                                        <Setter.Value>
                                                            <DropShadowEffect BlurRadius="4" ShadowDepth="0" Opacity="0.8" Color="#000000" />
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding CompletionStatus.Name}" Value="Playing">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                            <Setter Property="Fill" Value="#33FFFFFF" />
                                                            <Setter Property="Stroke" Value="#F2F2F2" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding CompletionStatus.Name}" Value="On Hold">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                            <Setter Property="Fill" Value="#33FFFFFF" />
                                                            <Setter Property="Stroke" Value="#F5A623" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding CompletionStatus.Name}" Value="Played">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                            <Setter Property="Fill" Value="#E9B824" />
                                                            <Setter Property="Stroke" Value="#8A6508" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding CompletionStatus.Name}" Value="Beaten">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                            <Setter Property="Fill" Value="#10B981" />
                                                            <Setter Property="Stroke" Value="#0A6E4E" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding CompletionStatus.Name}" Value="Completed">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                            <Setter Property="Fill" Value="#E0115F" />
                                                            <Setter Property="Stroke" Value="#8A0A38" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding CompletionStatus.Name}" Value="Abandoned">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                            <Setter Property="Fill" Value="#5A5F66" />
                                                            <Setter Property="Stroke" Value="#3A3E44" />
                                                            <Setter Property="Opacity" Value="0.75" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Path.Style>
                                        </Path>
                                    </Viewbox>
                                </Grid>

                                <!--
                                    Hover controls. Play moves to sit above the info button, at the
                                    info button's size, because the trophy now owns the bottom-right
                                    corner permanently. The button keeps its own style, so it still
                                    looks like itself, just smaller and stacked.
                                -->
                                <Grid Name="GameControls" Visibility="Collapsed">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <Viewbox Grid.Column="0" VerticalAlignment="Bottom" HorizontalAlignment="Left">
                                        <StackPanel Orientation="Vertical" Margin="8,0,0,8">
                                            <Button Name="PART_ButtonPlay" Content="&#xf5b0;" Width="32" Height="32" Margin="0,0,6,6"
                                                    Style="{DynamicResource GridViewPlayButton}" />
                                            <!--
                                                Edit, styled as the info button so the stack reads as
                                                one set of controls. Upstream only ever used
                                                PART_ButtonPlay and PART_ButtonInfo on a tile, so
                                                whether Playnite wires an edit button here is unproven
                                                rather than documented. If it turns out inert, the
                                                button has to go: a control that looks live and does
                                                nothing is worse than no control.
                                            -->
                                            <Button Name="PART_ButtonEditGame" Content="&#xe713;" Width="32" Height="32" Margin="0,0,6,6"
                                                    Style="{DynamicResource GridViewInfoButton}" />
                                            <Button Name="PART_ButtonInfo" Content="&#xedb1;" Width="32" Height="32" Margin="0,0,6,0"
                                                    Style="{DynamicResource GridViewInfoButton}" />
                                        </StackPanel>
                                    </Viewbox>
                                </Grid>
                            </Grid>
                            
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding ElementName=EnableHighlightAnimation, Path=Tag}" Value="True" />
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Tag" Value="True" TargetName="ShineBackground" />
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsVisible}" Value="True" />
                                <Condition Binding="{Binding Source={x:Static p:PlayniteApplication.Current}, Path=IsActive}" Value="True" />
                                <Condition Binding="{Binding ElementName=EnablePlayingGlowBorder, Path=Tag}" Value="True" />
                                <Condition Binding="{Binding IsRunning}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Tag" Value="True" TargetName="GlowPlayingBorder" />
                            <Setter Property="Visibility" Value="Visible" TargetName="GlowPlayingBorder" />
                        </MultiDataTrigger>
                        <DataTrigger Binding="{Binding IsInstalled}" Value="False">
                            <Setter Property="Content" Value="&#xe118;" TargetName="PART_ButtonPlay" />
                        </DataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsInstalled}" Value="False" />
                                <Condition Binding="{Settings DarkenUninstalledGamesGrid}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Opacity" Value="1" TargetName="SelectedGameBorder" />
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding CoverImage}" Value="{x:Null}" />
                                <Condition Binding="{Settings ShowNameEmptyCover}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible" TargetName="TextGameName" />
                        </MultiDataTrigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Visibility" Value="Visible" TargetName="GameControls" />
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="BorderMouseOver" Storyboard.TargetProperty="Opacity" To="0.24" Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <QuadraticEase EasingMode="EaseInOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="BorderMouseOver" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <QuadraticEase EasingMode="EaseInOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CoverIsPlayingAnimatedBackground" TargetType="Border">
        <Style.Resources>
            <Color x:Key="BackgroundColor1">#26ffffff</Color>
            <Color x:Key="BackgroundColor2">#00000000</Color>
            <Color x:Key="BackgroundColor3">#99000000</Color>
        </Style.Resources>
        <Setter Property="Background" x:Name="AnimatedBackground">
            <Setter.Value>
                <SolidColorBrush Color="{StaticResource BackgroundColor2}" />
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="True">
                <DataTrigger.EnterActions>
                    <BeginStoryboard x:Name="Animation">
                        <Storyboard Timeline.DesiredFrameRate="20">
                            <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(Background).(SolidColorBrush.Color)" FillBehavior="HoldEnd" AutoReverse="True"
                                    RepeatBehavior="Forever">
                                <ColorAnimationUsingKeyFrames.KeyFrames>
                                    <LinearColorKeyFrame Value="{StaticResource BackgroundColor1}" KeyTime="00:00:00.00" />
                                    <LinearColorKeyFrame Value="{StaticResource BackgroundColor1}" KeyTime="00:00:00.10" />
                                    <LinearColorKeyFrame Value="{StaticResource BackgroundColor2}" KeyTime="00:00:00.80" />
                                    <LinearColorKeyFrame Value="{StaticResource BackgroundColor3}" KeyTime="00:00:01.50" />
                                    <LinearColorKeyFrame Value="{StaticResource BackgroundColor3}" KeyTime="00:00:01.55" />
                                </ColorAnimationUsingKeyFrames.KeyFrames>
                            </ColorAnimationUsingKeyFrames>
                        </Storyboard>
                    </BeginStoryboard>
                </DataTrigger.EnterActions>
                <DataTrigger.ExitActions>
                    <RemoveStoryboard BeginStoryboardName="Animation" />
                </DataTrigger.ExitActions>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="AnimatedCoverShineBackground" TargetType="Border">
        <Setter Property="Opacity" Value="0.00" />
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="1.0, 0.0" EndPoint="0.0, 1.0">
                    <GradientStop Color="#00FFFFFF" Offset="-0.40" />
                    <GradientStop Color="#3DFFFFFF" Offset="-0.20" />
                    <GradientStop Color="#66FFFFFF" Offset="0" />
                    <GradientStop Color="Transparent" Offset="0.56" />
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="True">
                <DataTrigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard Timeline.DesiredFrameRate="30">
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1.10" Duration="0:0:0.4">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="Background.(GradientBrush.GradientStops)[0].(GradientStop.Offset)" To="1.00" Duration="0:0:0.8">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseInOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="Background.(GradientBrush.GradientStops)[1].(GradientStop.Offset)" To="1.10" Duration="0:0:0.8">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseInOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="Background.(GradientBrush.GradientStops)[2].(GradientStop.Offset)" To="1.30" Duration="0:0:0.8">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseInOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="Background.(GradientBrush.GradientStops)[3].(GradientStop.Offset)" To="1.30" Duration="0:0:0.8">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseInOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </DataTrigger.EnterActions>
                <DataTrigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0.00" Duration="0:0:0.4">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="Background.(GradientBrush.GradientStops)[0].(GradientStop.Offset)" To="-0.40" Duration="0:0:0.6">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseInOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="Background.(GradientBrush.GradientStops)[1].(GradientStop.Offset)" To="-0.20" Duration="0:0:0.6">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseInOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="Background.(GradientBrush.GradientStops)[2].(GradientStop.Offset)" To="0" Duration="0:0:0.6">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseInOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="Background.(GradientBrush.GradientStops)[3].(GradientStop.Offset)" To="0.56" Duration="0:0:0.6">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseInOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </DataTrigger.ExitActions>
            </DataTrigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>